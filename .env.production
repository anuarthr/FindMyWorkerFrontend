# Producción — FindMyWorker
# Host del backend desplegado en Render. El frontend deriva la URL REST
# (`${VITE_API_BASE_URL}/api/`) y el WebSocket (`wss://host`) desde aquí.
# Sin slash final.
VITE_API_BASE_URL=https://findmyworker-backend.onrender.com
